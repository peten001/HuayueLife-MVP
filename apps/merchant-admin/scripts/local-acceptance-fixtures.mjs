// Isolated UI fixtures. Never imported by application/runtime code.
export function createAcceptanceState() {
  const businessDate = '2026-09-05';
  const stamp = '2026-09-05T12:00:00.000Z';
  const capabilities = ['onlineOrderEnabled','productDisplayEnabled','pickupEnabled','deliveryEnabled','qrOrderEnabled','tableManagementEnabled','printerEnabled','chatEnabled'].map(code => ({code,isEnabled:true}));
  const merchant = {id:'local-9000',nameZh:'本地验收 · 云桥餐厅',nameVi:'Nhà hàng thử nghiệm',nameEn:'Local acceptance restaurant',status:'ACTIVE',merchantMode:'ORDER',capabilities,reportFeatureEnabled:false};
  const staff = {id:'local-owner',displayName:'本地验收店主',username:'local',role:'OWNER',mustChangePassword:false,merchant};
  const categories = ['川味热菜','清爽凉菜','汤品与主食','酒水饮料'].map((nameZh,i) => ({id:`c${i}`,nameZh,nameVi:['Món nóng Tứ Xuyên','Món nguội','Canh và cơm','Đồ uống'][i],nameEn:['Hot dishes','Cold dishes','Soup and rice','Drinks'][i],sortOrder:i,status:'ACTIVE'}));
  const names = ['回锅肉','宫保鸡丁','鱼香肉丝','麻婆豆腐','水煮牛肉','酸辣蕨根粉','蒜泥白肉','拍黄瓜','番茄鸡蛋汤','米饭','炒面','柠檬茶'];
  const products = Array.from({length:28},(_,i) => ({id:`p${i}`,categoryId:`c${Math.floor(i/7)}`,category:categories[Math.floor(i/7)],nameZh:i===25?'多语言长名称展示测试：四川传统风味双椒鲜香特色家常菜':names[i%names.length],nameVi:i===25?'Món ăn Tứ Xuyên truyền thống với hai loại ớt và hương vị thơm ngon đặc biệt dành cho gia đình':'Món ăn thử nghiệm '+(i+1),nameEn:'Local sample dish '+(i+1),description:'本地隔离展示数据，不是线上商品',priceVnd:String(25000+(i%9)*15000),unit:i%4===3?'杯':'份',sortOrder:i,status:i%9===8?'SOLD_OUT':i%13===12?'OFF_SALE':'ON_SALE',imageUrl:i===0?'/src/assets/huayue-miniapp-logo.png':i===1?'/__local/missing-image.png':null,createdAt:stamp,updatedAt:stamp}));
  const tables = Array.from({length:12},(_,i) => ({id:`t${i}`,tableNo:`A${String(i+1).padStart(2,'0')}`,tableName:i===0?'一楼靠窗':'大厅',seatCount:4,status:'ACTIVE',qrToken:`local-only-${i}`,createdAt:stamp,updatedAt:stamp}));
  const orders = Array.from({length:26},(_,i) => {
    const product = products[i%products.length];
    const createdAt = `${businessDate}T${String(3+i%16).padStart(2,'0')}:15:00.000Z`;
    return {id:`o${i}`,orderNo:`LOCAL260905${String(i+1).padStart(6,'0')}`,merchantId:merchant.id,orderType:['DINE_IN','PICKUP','DELIVERY'][i%3],status:i<21?'COMPLETED':i===21?'CANCELLED':'PENDING_ACCEPTANCE',settlementStatus:i<21?'SETTLED':'UNSETTLED',createdAt,updatedAt:createdAt,completedAt:i<21?createdAt:null,tableNoSnapshot:tables[i%12].tableNo,table:tables[i%12],contactName:'本地测试顾客',contactPhone:'',deliveryAddress:'本地隔离示例地址',customerRemark:i===1?'少辣，不加香菜':'',cancelReason:i===21?'退空订单自动取消':null,itemAmountVnd:String(Number(product.priceVnd)*2),deliveryFeeVnd:'0',totalAmountVnd:String(Number(product.priceVnd)*2),items:[{id:`oi${i}`,productId:product.id,productNameZhSnapshot:product.nameZh,productNameViSnapshot:product.nameVi,productNameEnSnapshot:product.nameEn,unitPriceVnd:product.priceVnd,quantity:2,subtotalVnd:String(Number(product.priceVnd)*2),remark:''}],statusLogs:[],printLogs:[],chatConversation:i===25?{id:'local-chat',status:'OPEN',merchantUnreadCount:2,customerUnreadCount:0}:null};
  });
  const settlements = orders.filter(o=>o.status==='COMPLETED').map((order,i) => ({settlementId:`order:${order.id}`,kind:'ORDER',orderType:order.orderType,status:'COMPLETED',businessDate,settledAt:order.completedAt,tableSessionId:null,tableId:order.table.id,tableName:order.orderType==='DINE_IN'?order.tableNoSnapshot:null,orderIds:[order.id],orderNos:[order.orderNo],orderCount:1,itemQuantity:2,items:order.items.map(item => ({...item,productNameZh:item.productNameZhSnapshot,productNameVi:item.productNameViSnapshot,productNameEn:item.productNameEnSnapshot,imageUrl:null})),originalAmountVnd:order.totalAmountVnd,discountAmountVnd:i%4===0?'5000':'0',roundingAmountVnd:'0',finalReceivableVnd:String(Number(order.totalAmountVnd)-(i%4===0?5000:0)),paymentMethod:i%3===0?'BANK_TRANSFER':'CASH',sourceOrders:[{...order,paymentMethod:i%3===0?'BANK_TRANSFER':'CASH'}],invariantViolations:[]}));
  const staffList=[staff,{id:'local-manager',username:'0900000001',displayName:'本地经理',role:'MANAGER',status:'ACTIVE',createdAt:stamp,updatedAt:stamp},{id:'local-staff',username:'0900000002',displayName:'本地员工',role:'STAFF',status:'ACTIVE',createdAt:stamp,updatedAt:stamp}].map(s=>({...s,status:'ACTIVE',createdAt:stamp,updatedAt:stamp}));
  let profile = {...merchant,contactName:'本地测试联系人',contactPhone:'',province:'胡志明市',city:'胡志明市',district:'测试区',addressDetail:'本地隔离环境，非真实地址',latitude:'10.77',longitude:'106.69',images:[],businessHours:Object.fromEntries(['monday','tuesday','wednesday','thursday','friday','saturday','sunday'].map(k=>[k,['09:00-14:00','17:00-02:00']])),notice:'本地验收数据。所有更改仅保存在此次本地服务内存。',minimumDeliveryAmountVnd:'0',deliveryFeeVnd:'15000',deliveryRadiusKm:'3',dineInEnabled:true,pickupEnabled:true,deliveryEnabled:true,isVisibleOnClient:true,homepageCategoryKeys:[]};
  const sessions = tables.slice(0,3).map((table,i)=>({id:`session${i}`,tableId:table.id,tableNo:table.tableNo,tableName:table.tableName,sessionNo:`LOCAL-SESSION-20260905-VERY-LONG-IDENTIFIER-${i}`,status:'OPEN',openedAt:stamp,closedAt:null,orderCount:1,itemCount:2,unfinishedOrderCount:1,totalAmountVnd:orders[23+i].totalAmountVnd,latestOrderAt:orders[23+i].createdAt,orders:[orders[23+i]],items:orders[23+i].items}));
  const voids=[];
  const flags={taskCenterEnabled:true,automaticCreationEnabled:false,executionEnabled:false,lanPrintingEnabled:false,legacyPrintingEnabled:false,merchantPrintingEnabled:true,executionState:'CONNECTOR_PENDING'};
  const printing={printers:[],rules:[],templates:[],jobs:[],terminals:[]};
  const sum=(list,key)=>list.reduce((n,r)=>n+BigInt(r[key]||'0'),0n).toString();
  function analytics(from=businessDate,to=from,orderType='') {
    const selected=settlements.filter(s=>s.businessDate>=from&&s.businessDate<=to&&(!orderType||s.orderType===orderType));
    const funds={grossAmountVnd:sum(selected,'originalAmountVnd'),discountAmountVnd:sum(selected,'discountAmountVnd'),roundingAmountVnd:sum(selected,'roundingAmountVnd'),netSettledAmountVnd:sum(selected,'finalReceivableVnd'),cashRevenueVnd:sum(selected.filter(s=>s.paymentMethod==='CASH'),'finalReceivableVnd'),bankTransferRevenueVnd:sum(selected.filter(s=>s.paymentMethod==='BANK_TRANSFER'),'finalReceivableVnd'),unrecordedRevenueVnd:'0'};
    const count=Math.round((Date.parse(to)-Date.parse(from))/86400000)+1;
    const trend=Array.from({length:count===1?24:count},(_,i)=>{const key=count===1?`${String(i).padStart(2,'0')}:00`:new Date(Date.parse(from)+i*86400000).toISOString().slice(0,10);const data=selected.filter(s=>count===1?(new Date(s.settledAt).getUTCHours()+7)%24===i:s.businessDate===key);return{key,label:key,settlementCount:data.length,revenueVnd:sum(data,'finalReceivableVnd')};});
    const topDishes=products.map(p=>{const data=selected.flatMap(s=>s.items).filter(i=>i.productId===p.id);return{key:p.id,productId:p.id,name:p.nameZh,imageUrl:null,quantity:data.reduce((n,i)=>n+i.quantity,0),revenueVnd:sum(data,'subtotalVnd'),previousQuantity:0,changePercent:null};}).filter(p=>p.quantity).sort((a,b)=>b.quantity-a.quantity).slice(0,10);
    const timeDistribution=Array.from({length:84},(_,i)=>{const weekday=Math.floor(i/12),startHour=i%12*2;const data=selected.filter(s=>{const local=new Date(Date.parse(s.sourceOrders[0].createdAt)+7*3600000);return(local.getUTCDay()+6)%7===weekday&&Math.floor(local.getUTCHours()/2)*2===startHour;});return{weekday,startHour,endHour:startHour+2,settlementCount:data.length,revenueVnd:sum(data,'finalReceivableVnd')};});
    const peak=timeDistribution.reduce((best,row)=>row.settlementCount>best.settlementCount?row:best,{settlementCount:0});
    return {generatedAt:new Date().toISOString(),currency:'VND',period:{startDate:from,endDate:to,previousStartDate:new Date(Date.parse(from)-count*86400000).toISOString().slice(0,10),previousEndDate:new Date(Date.parse(from)-86400000).toISOString().slice(0,10),dayCount:count,granularity:count===1?'hour':'day',timeZone:'Asia/Ho_Chi_Minh'},overview:{revenueVnd:funds.netSettledAmountVnd,settlementCount:selected.length,averageOrderValueVnd:selected.length?String(Math.round(Number(funds.netSettledAmountVnd)/selected.length)):'0',funds,previous:{revenueVnd:'0',settlementCount:0,averageOrderValueVnd:'0'},comparison:{revenuePercent:null,settlementCountPercent:null,averageOrderValuePercent:null},topDish:topDishes[0]||null},trend,timeDistribution,peakPeriod:peak.settlementCount?peak:null,topDishes};
  }
  function respond(path,query,method,body,role='OWNER') {
    if(path==='/merchant/me') return {user:{sub:staff.id,role,username:staff.username,merchantId:merchant.id,merchant,mustChangePassword:false}};
    if(path==='/merchant/auth/login') return {accessToken:'LOCAL_UI_ONLY',staff:{...staff,role}};
    if(path==='/merchant/orders/business-day-summary') return {businessDate,orderCount:orders.length,settlementCount:settlements.length,totalRevenueVnd:sum(settlements,'finalReceivableVnd')};
    if(path==='/merchant/analytics') return analytics(query.get('dateFrom')||businessDate,query.get('dateTo')||query.get('dateFrom')||businessDate);
    if(path==='/merchant/profile') {if(method==='PATCH') profile={...profile,...body};return profile;}
    if(path==='/merchant/orders/summary'){const date=query.get('date')||businessDate;const result=analytics(date,date,query.get('orderType')||'');const {funds,settlementCount}=result.overview;return{COMPLETED:{count:settlementCount,settlementCount,amountVnd:funds.netSettledAmountVnd,...funds}};}
    if(path==='/merchant/orders') return orders.filter(o=>(!query.get('date')||query.get('date')===businessDate)&&(!query.get('status')||o.status===query.get('status'))&&(!query.get('orderType')||o.orderType===query.get('orderType')));
    if(path.startsWith('/merchant/orders/')) return orders.find(o=>o.id===path.split('/').at(-1)) || null;
    if(path==='/merchant/settlements') {const list=settlements.filter(s=>(!query.get('date')||s.businessDate===query.get('date'))&&(!query.get('orderType')||s.orderType===query.get('orderType'))&&(!query.get('paymentMethod')||s.paymentMethod===query.get('paymentMethod'))&&(!query.get('search')||`${s.orderNos.join(' ')} ${s.orderIds.join(' ')}`.toLocaleLowerCase().includes(query.get('search').toLocaleLowerCase()))).sort((a,b)=>Date.parse(b.settledAt)-Date.parse(a.settledAt));const page=Number(query.get('page')||1),pageSize=Number(query.get('pageSize')||20);return{items:list.slice((page-1)*pageSize,page*pageSize),total:list.length,page,pageSize,hasMore:page*pageSize<list.length};}
    if(path.startsWith('/merchant/settlements/')) return settlements.find(s=>s.settlementId===decodeURIComponent(path.split('/').at(-1)))||null;
    if(path==='/merchant/table-sessions/open') return{sessions};
    if(path.startsWith('/merchant/table-sessions/')) return{session:sessions.find(s=>s.id===path.split('/').at(-1))};
    if(path==='/merchant/order-voids')return{items:voids,total:voids.length,hasMore:false};
    if(path.startsWith('/merchant/order-voids/')) {const target=decodeURIComponent(path.split('/')[3]);const settlement=settlements.find(s=>s.settlementId===target||s.orderIds.includes(target.replace('order:','')));if(!settlement)throw new Error('本地记录不存在');const preview={target,version:'local-version',settlement,affectedOrderIds:settlement.orderIds,affectedOrderNos:settlement.orderNos,businessDayImpacts:[],settlementImpact:{businessDate,settlementCount:1,revenueVnd:settlement.finalReceivableVnd}};if(method==='GET')return preview;const record={...preview,operationId:`local-void-${voids.length}`,voidedAt:new Date().toISOString(),actor:{id:staff.id,displayName:staff.displayName},reason:body.reason,note:body.note};voids.push(record);settlements.splice(settlements.indexOf(settlement),1);for(const id of settlement.orderIds){const index=orders.findIndex(o=>o.id===id);if(index>=0)orders.splice(index,1);}return record;}
    if(path==='/merchant/printing/feature-state')return flags;
    if(path==='/merchant/printing/settings'||path==='/merchant/printing/automatic-creation')return{id:'local',printingEnabled:true,automaticCreationEnabled:false,featureFlags:flags};
    if(path==='/merchant/printing/routing')return{configured:false,checkoutDefaultPrinterId:null,defaultKitchenPrinterId:null,frontDeskPrinters:[],kitchenPrinters:[]};
    if(path==='/merchant/printing/cloud-execution-state')return{enabled:false,providers:{FEIE:{enabled:false,configured:false},YILIAN:{enabled:false,configured:false}}};
    if(path.includes('/templates/current/'))return null;
    const collection = path.match(/^\/merchant\/(categories|products|tables|staff)(?:\/([^/]+))?(?:\/(status|enable|disable|reset-password))?$/);
    if(collection){const [,kind,id,action]=collection;const list={categories,products,tables,staff:staffList}[kind];if(method==='GET')return id?list.find(r=>r.id===id):list;if(method==='POST'&&!id){const row={id:`local-${Date.now()}`,status:kind==='products'?'ON_SALE':'ACTIVE',sortOrder:0,createdAt:stamp,updatedAt:stamp,...body};if(kind==='products')row.category=categories.find(c=>c.id===row.categoryId);list.push(row);return row;}const row=list.find(r=>r.id===id);if(!row)throw new Error('本地记录不存在');if(method==='DELETE'){list.splice(list.indexOf(row),1);return{};}Object.assign(row,body);if(kind==='products')row.category=categories.find(c=>c.id===row.categoryId);if(action==='enable')row.status='ACTIVE';if(action==='disable')row.status='DISABLED';return row;}
    const printList=path.match(/^\/merchant\/printing\/(printers|rules|templates|jobs|terminals)$/);if(printList){if(method==='GET')return printing[printList[1]];throw new Error('本地验收不创建打印任务或设备');}
    if(path==='/merchant/printers')return[];
    throw new Error(`本地隔离环境未启用此操作：${method} ${path}`);
  }
  return {respond,staff,businessDate};
}
